{
  "name": "form-builder",
  "version": "1.0.0",
  "description": "Shopify Form Builder App",
  "private": true,
  "scripts": {
    "deploy": "shopify app deploy"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}

